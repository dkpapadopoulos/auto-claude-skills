# Regex fixtures for skill: design-debate
# Format: one directive per non-empty, non-comment line.
#   MATCH: <prompt>     — regex from config/default-triggers.json for this skill must match
#   NO_MATCH: <prompt>  — regex must NOT match
# Tested deterministically by tests/test-regex-fixtures.sh (zero LLM cost).
# NO_MATCH line is verbatim-borrowed from supply-chain-investigation MATCH fixture.

MATCH: let's debate the tradeoffs between a monolith and microservices architecture
MATCH: compare the two approaches and give me the pro-con breakdown for each
MATCH: what are the alternatives to using a message queue for this use case

NO_MATCH: was the npm package compromised in the latest attack
# The `weigh` clause had NO fixture coverage in either direction, so a narrowing that
# sent real deliberation prompts nowhere was invisible to the suite. Both sides pinned now.
MATCH: weigh these two options and tell me which is better
MATCH: help me weigh up the different approaches
NO_MATCH: ask codex to weigh in on this approach
# The determiner list was incomplete AND irregular: a bare `a` branch admitted any
# a-word by prefix, so "weigh another approach" matched accidentally rather than by
# design, while "weigh my options" missed. Each determiner is now a whole word.
MATCH: weigh my options
MATCH: weigh those options
MATCH: weigh possible approaches
MATCH: weigh pros and cons of this approach

# Contract triggers (participants deliberately INTERACT). The topic-word triggers above
# never encoded the contract itself: db-2 of the intent-routing probe -- "have two agents
# argue for and against event sourcing here" -- failed in BOTH arms. The NO_MATCH lines
# borrow panel's contract cases verbatim: panel is the contract-opposite (independence,
# no cross-talk), so those are the decoys that make the boundary load-bearing.
MATCH: have two agents argue for and against event sourcing here
MATCH: have the models argue this out between them
MATCH: let them rebut each other's answers
MATCH: i want a back and forth between two models on this
MATCH: have gemini and codex challenge each other until they converge

NO_MATCH: get independent answers from several models and show them to me unmerged
NO_MATCH: run a standalone panel on this question; return independent raw answers
NO_MATCH: argue for the current design in the pr description
NO_MATCH: the two services counter each other's backpressure signals
NO_MATCH: we need to challenge the assumption in the spec
